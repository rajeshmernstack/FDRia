{
  "name": "amazona",
  "version": "1.0.0",
  "description": "1. Introducction\r 2. Install Tools\r 3. Create React App\r 4. Create Git Repository",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install  && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
